---
- hosts: test-node
  tasks:
    - shell: "ls -al {{ ansible_user_dir }}/"
    - include_vars: "{{ ansible_user_dir }}/test.yaml"
    - name: Post display var test_var
      debug:
        var: '{{ test_var }}'
